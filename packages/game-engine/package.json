{
  "name": "@media-timeline-game/game-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --emitDeclarationOnly",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo \"No lint configured for game-engine\""
  },
  "dependencies": {
    "@media-timeline-game/shared-types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}

