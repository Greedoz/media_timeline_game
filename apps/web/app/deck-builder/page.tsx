export default function DeckBuilderPage() {
  return (
    <main>
      <section className="panel">
        <h1>Deck Builder</h1>
        <p className="subtle">
          Use `POST /api/decks` and `POST /api/decks/:id/cards/upload` to create decks and upload media in this MVP
          scaffold.
        </p>
      </section>
    </main>
  );
}

